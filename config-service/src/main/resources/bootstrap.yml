server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VictorCov/ms-course-youtube.git
          default-label: 02-config
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_TOKEN}


  security:
    user:
      name: root
      password: s3cr3t